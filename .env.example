# Public domain name that should resolve to the server running this stack
DOMAIN=example.com

# Email used for Let's Encrypt notifications and certificate registration
EMAIL=admin@example.com

# Optional: tokens for Telegram notifications (used by the PHP app)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# Optional: API key for ipdata.co IP geolocation (used by the PHP app)
# IPDATA_API_KEY=
